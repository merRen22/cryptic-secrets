---
import '../styles/global.css';
import Button from './Button.astro';

interface Props {
  data?: string;
}

const { data } = Astro.props;
---

<div class="flex items-center justify-center min-h-screen">
  <div class="card bg-base-100 shadow-2xl w-full max-w-sm">
    <div class="card-body gap-6" style="padding: 2rem;">
      <h2 class="text-3xl font-bold">Enter Password</h2>
      <fieldset class="fieldset">
        <input
          type="password"
          id="key"
          placeholder="Password"
          autocomplete="off"
          class="input input-bordered w-full"
        />
      </fieldset>
      <Button class="btn btn-neutral w-full mt-4" id="decrypt">Decrypt</Button>
    </div>
    <pre id="content" class="whitespace-pre-wrap font-mono leading-relaxed hidden text-white p-6"></pre>
  </div>
</div>
